describe( 'Array', () => {
	describe( '.prototype', () => {} );
	describe( '.from()', () => {} );
	describe( '#length', () => {} );
	describe( '#concat()', () => {} )
} );

describe( 'selectors', () => {
	describe( 'isSitePreviewable()', () => {} );
} );
